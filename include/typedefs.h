#ifndef TYPEDEFS_H
#define TYPEDEFS_H

namespace chicago_crime{

enum QuestionType{
    /* questiontype enum definitions*/
    TIME,
    DISTRICT,
    LOCATION
};

enum LocationType{
    /* locationtype enum definitions*/
    EXTERIOR,
    INTERIOR
};

}

#endif // TYPEDEFS_H
